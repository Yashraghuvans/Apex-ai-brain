<?xml version="1.0" encoding="UTF-8"?>
<LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>62.0</apiVersion>
    <isExposed>true</isExposed>
    <masterLabel>{{componentName}}</masterLabel>
    <description>{{description}}</description>
    
    <!-- Configure where this component can be used -->
    <targets>
        <!-- Lightning App Page -->
        <target>lightning__AppPage</target>
        
        <!-- Record Page (Lightning Record Page) -->
        <target>lightning__RecordPage</target>
        
        <!-- Flow Screen (use in Flow Designer) -->
        <target>lightning__FlowScreen</target>
        
        <!-- Messaging Feature (via Lightning Message Service) -->
        <target>lightning__Messaging</target>
    </targets>

    <!-- Component Configuration Properties -->
    <!-- These can be configured in App Builder or Flow -->
    <targetConfigs>
        <!-- App Page Configuration -->
        <targetConfig targets="lightning__AppPage">
            <property name="recordId" type="String" label="Record ID" description="The record ID context" />
            <property name="objectApiName" type="String" label="Object API Name" description="The object API name" />
        </targetConfig>

        <!-- Record Page Configuration -->
        <targetConfig targets="lightning__RecordPage">
            <property name="recordId" type="String" label="Record ID" description="The ID of the record" />
            <property name="objectApiName" type="String" label="Object API Name" description="The API name of the object" />
        </targetConfig>

        <!-- Flow Screen Configuration -->
        <targetConfig targets="lightning__FlowScreen">
            <property name="recordId" type="String" label="Record ID" />
        </targetConfig>
    </targetConfigs>

    <!-- Supported Lightning Interface: recordPageHeaderActions -->
    <!-- Allows component to add actions to record page header -->
    <supportedFormFactors>
        <supportedFormFactor type="Large" />
        <supportedFormFactor type="Small" />
    </supportedFormFactors>
</LightningComponentBundle>
